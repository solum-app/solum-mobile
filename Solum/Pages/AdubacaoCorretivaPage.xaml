<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Solum.Pages.AdubacaoCorretivaPage"
    xmlns:handlers="clr-namespace:Solum.Handlers"
    Title="{Binding PageTitle}">
    <ScrollView BackgroundColor="{StaticResource colorBackground}">
        <StackLayout Spacing="0">
            <Label Style="{StaticResource headerStyle}"
                Text="RECOMENDAÇÃO DE ADUBAÇÃO CORRETIVA"/>
            <Grid BackgroundColor="White"
                ColumnSpacing="0"
                HeightRequest="72"
                RowSpacing="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <BoxView Style="{StaticResource separatorStyle}"
                    Grid.ColumnSpan="2"
                    VerticalOptions="Start"/>
                <Label Grid.Column="1"
                    Grid.Row="0"
                    Margin="0,8,0,0"
                    Text="Quantidade de P2O5"
                    TextColor="{StaticResource colorTextPrimary}"
                    VerticalOptions="Center"
                    VerticalTextAlignment="Center"/>
                <Label Grid.Column="1"
                    Grid.Row="1"
                    Margin="0,0,0,8"
                    Text="{Binding P2O5, StringFormat='{0:0.##} kg/ha'}"
                    TextColor="{StaticResource colorTextSecundary}"
                    VerticalOptions="Center"
                    VerticalTextAlignment="Center"/>
                <BoxView Style="{StaticResource separatorStyle}"
                    Grid.Column="1"
                    Grid.Row="1"
                    VerticalOptions="End"/>
            </Grid>
            <Grid BackgroundColor="White"
                ColumnSpacing="0"
                HeightRequest="72"
                RowSpacing="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Label Grid.Column="1"
                    Grid.Row="0"
                    Margin="0,8,0,0"
                    Text="Quantidade de K2O"
                    TextColor="{StaticResource colorTextPrimary}"
                    VerticalOptions="Center"
                    VerticalTextAlignment="Center"/>
                <Label Grid.Column="1"
                    Grid.Row="1"
                    Margin="0,0,0,8"
                    Text="{Binding K2O, StringFormat='{0:0.##} kg/ha'}"
                    TextColor="{StaticResource colorTextSecundary}"
                    VerticalOptions="Center"
                    VerticalTextAlignment="Center"/>
            </Grid>
            <BoxView Style="{StaticResource separatorStyle}"
                VerticalOptions="End"/>
            <Button BackgroundColor="{StaticResource colorAccent}"
                Command="{Binding SalvarCommand}"
                IsVisible="{Binding EnableButton}"
                Margin="16"
                Text="Salvar"
                IsEnabled="{Binding IsBusy, Converter={x:Static handlers:IverseBoolConverter.Instance}}"
                TextColor="{StaticResource colorTextPrimary}"
                VerticalOptions="EndAndExpand"/>
        </StackLayout>
    </ScrollView>
</ContentPage>