<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:effects="clr-namespace:Solum.Effects"
	xmlns:renderers="clr-namespace:Solum.Renderers"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	x:Class="Solum.Pages.SemeaduraPage"
	Title="{Binding PageTitle}">

  <ScrollView Grid.Row="0" BackgroundColor="{StaticResource colorBackground}">
    <StackLayout Spacing="0">
      <Label TextColor="{StaticResource colorTextSecundary}" Text="INFORMAÇÕES ADUBAÇÃO DE SEMEADURA">
        <Label.Margin>
          <OnPlatform x:TypeArguments="Thickness"
						iOS="16,24,16,4"
						Android="16, 13"/>
        </Label.Margin>
        <Label.FontSize>
          <OnPlatform x:TypeArguments="x:Double"
						iOS="13"
						Android="14"/>
        </Label.FontSize>
      </Label>

      <Grid HeightRequest="45" BackgroundColor="White" ColumnSpacing="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="16"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
	        <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <BoxView Grid.ColumnSpan="3" BackgroundColor="{StaticResource colorSeparator}" VerticalOptions="Start">
          <BoxView.HeightRequest>
            <OnPlatform x:TypeArguments="x:Double"
							iOS="0.5"
							Android="0.3"/>
          </BoxView.HeightRequest>
        </BoxView>
        <Label Text="Expectativa de Rendimento" VerticalOptions="Center" Grid.Column="1" TextColor="{StaticResource colorTextPrimary}"/>
        <renderers:CustomPicker
          ItemsSource="{Binding Expectativas}"
          SelectedItem="{Binding ExpectativaSelected}"
          VerticalOptions="Center"
          Grid.Column="2"
          Margin="16,0"
          HasBorder="false"
          PlaceholderColor="{StaticResource colorTextHint}"
		  Title="t/ha"
          TextAlignment="Right">
          <renderers:CustomPicker.TextSize>
            <OnPlatform x:TypeArguments="x:Double"
              Android="14" iOS="17"/>
          </renderers:CustomPicker.TextSize>
        </renderers:CustomPicker>
        <BoxView BackgroundColor="{StaticResource colorSeparator}" Grid.Column="1" Grid.ColumnSpan="2" VerticalOptions="End">
          <BoxView.HeightRequest>
            <OnPlatform x:TypeArguments="x:Double"
							iOS="0.5"
							Android="0.3"/>
          </BoxView.HeightRequest>
        </BoxView>
      </Grid>

      <Grid HeightRequest="45" BackgroundColor="White" ColumnSpacing="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="16"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Text="Cultura" VerticalOptions="Center" Grid.Column="1" TextColor="{StaticResource colorTextPrimary}"/>
        <renderers:CustomPicker
         ItemsSource="{Binding Culturas}"
         SelectedItem="{Binding CulturaSelected}"
         VerticalOptions="Center"
         Grid.Column="2"
         Margin="16,0"
         HasBorder="false"
         PlaceholderColor="{StaticResource colorTextHint}"
         TextAlignment="Right">
          <renderers:CustomPicker.TextSize>
            <OnPlatform x:TypeArguments="x:Double"
              Android="14" iOS="17"/>
          </renderers:CustomPicker.TextSize>
        </renderers:CustomPicker>
      </Grid>

      <BoxView BackgroundColor="{StaticResource colorSeparator}" VerticalOptions="End">
        <BoxView.HeightRequest>
          <OnPlatform x:TypeArguments="x:Double"
						iOS="0.5"
						Android="0"/>
        </BoxView.HeightRequest>
      </BoxView>

      <Label
				Text="AVISO! O nível de Potássio de sua análise estava baixo, para realizar a recomendação de semeadura o Solum vai adotar que você corrigiu para o nível adequado utilizando nossa recomendação corretiva."
				TextColor="{StaticResource colorTextPrimary}"
				Margin="16"
				FontSize="Small" IsVisible="{Binding IsPotassioBaixo}"/>

      <Button Text="Recomendação" Command="{Binding RecomendarCommand}" IsEnabled="true" Margin="16" BackgroundColor="{StaticResource colorAccent}" TextColor="{StaticResource colorTextPrimary}" VerticalOptions="EndAndExpand"/>
    </StackLayout>
  </ScrollView>
</ContentPage>