<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:handlers="clr-namespace:Solum.Handlers;assembly=Solum"
    xmlns:renderers="clr-namespace:Solum.Renderers;assembly=Solum"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Solum.Pages.FazendaDetalhesPage"
    BackgroundColor="{StaticResource colorBackground}"
    Title="{Binding PageTitle}">
    <ActivityIndicator HorizontalOptions="Center"
        IsRunning="{Binding IsLoading}"
        VerticalOptions="Center"/>
    <AbsoluteLayout x:Name="absoluteLayout"
        IsVisible="{Binding IsLoading, Converter={x:Static handlers:IverseBoolConverter.Instance}}">
        <Label AbsoluteLayout.LayoutBounds="0,0,1,1"
            AbsoluteLayout.LayoutFlags="All"
            FontSize="Medium"
            HorizontalOptions="Center"
            IsVisible="{Binding HasItems, Converter={x:Static handlers:IverseBoolConverter.Instance}}"
            Text="Nenhum talhão cadastrado"
            TextColor="{StaticResource colorTextSecundary}"
            VerticalOptions="Center"/>
        <renderers:BindableListView x:Name="talhoesList"
            AbsoluteLayout.LayoutBounds="0,0,1,1"
            AbsoluteLayout.LayoutFlags="All"
            HasUnevenRows="true"
            IsVisible="{Binding HasItems}"
            ItemClickedCommand="{Binding ItemTappedCommand}"
            ItemsSource="{Binding Talhoes}"
            SeparatorColor="{StaticResource colorSeparator}">
            <renderers:BindableListView>
                <x:Arguments>
                    <ListViewCachingStrategy>RecycleElement</ListViewCachingStrategy>
                </x:Arguments>
            </renderers:BindableListView>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid BackgroundColor="White"
                            Padding="15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Label Grid.Row="0"
                                Text="{Binding Nome, StringFormat='Talhão {0}'}"
                                TextColor="{StaticResource colorTextPrimary}"/>
                            <Label FontSize="Small"
                                Grid.Row="1"
                                Text="{Binding Area, Converter={x:Static handlers:AreaGTZeroConverter.Instance}}"
                                TextColor="{StaticResource colorTextSecundary}"
                                VerticalTextAlignment="Start"/>
                        </Grid>
                        <ViewCell.ContextActions>
                            <MenuItem Command="{Binding BindingContext.ShowEditTalhaoPageCommand, Source={x:Reference talhoesList}"
                                CommandParameter="{Binding .}"
                                Icon="ic_editar"
                                Text="Editar"/>
                            <MenuItem Command="{Binding BindingContext.DeleteTalhaoCommand, Source={x:Reference talhoesList}"
                                CommandParameter="{Binding .}"
                                Icon="ic_excluir"
                                IsDestructive="True"
                                Text="Excluir"/>
                        </ViewCell.ContextActions>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </renderers:BindableListView>
    </AbsoluteLayout>
</ContentPage>