<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:local="clr-namespace:Solum.Renderers;assembly=Solum"
			 xmlns:converter="clr-namespace:Solum.Handlers;assembly=Solum"
			 x:Class="Solum.Pages.FazendaCadastroPage">
	<StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand">

		<Entry x:Name="FazendaNomeEntry" Placeholder="Nome da Fazenda" Text="{Binding FazendaName}" />

		<local:CustomPicker x:Name="EstadoPicker" Title="Selecione um estado"
							ItemsSource="{Binding EstadoList}"
							SelectedItem="{Binding EstadoSelected}"
							SelectedIndex="{Binding EstadoSelectedIndex}"
							ItemSelectedCommand="{Binding UpdateCidadesListCommand}" />

		<local:CustomPicker x:Name="CidadePicker" Title="Selecione uma cidade"
							ItemsSource="{Binding CidadeList}"
							SelectedItem="{Binding CidadeSelected}"
							SelectedIndex="{Binding CidadeSelectedIndex}"
							IsEnabled="{Binding IsEstadoSelected}" />

		<Button x:Name="SalvarButton"
				Text="Salvar"
				Command="{Binding CadastrarFazendaCommand}"
				IsEnabled="{Binding IsCarregandoEstados, Converter={x:Static converter:IverseBoolConverter.Instance}}" />
	</StackLayout>
</ContentPage>